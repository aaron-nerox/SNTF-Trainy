<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.SNTF" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/orange_200</item>
        <item name="colorPrimaryVariant">@color/orange_500</item>
        <item name="colorOnPrimary">@color/orange_100</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/black_200</item>
        <item name="colorSecondaryVariant">@color/black_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/transparent</item>
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@drawable/window_back</item>
        <item name="shapeAppearanceSmallComponent">@style/ShapeAppearanceOverlay.TheX.small</item>
        <item name="shapeAppearanceMediumComponent">@style/ShapeAppearanceOverlay.TheX.medium</item>
        <item name="shapeAppearanceLargeComponent">@style/ShapeAppearanceOverlay.TheX.larg</item>
    </style>

    <style name="ThemeLauncher" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_screen</item>
        <item name="android:statusBarColor" tools:targetApi="l">@color/transparent</item>
    </style>

    <style name="Theme.SNTF.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.SNTF.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.SNTF.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>